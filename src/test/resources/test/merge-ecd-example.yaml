configuration:
  name: test-experiment
  author: junit
  
collection-reader:
  inherit: test.collection.reader

pipeline:
  - inherit: ecd.phase
    name: merge-phase
    options: |
      - inherit: test.first-phase-annotator-merge
